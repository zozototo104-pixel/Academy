# ============================================
# AACT Platform — متغيرات البيئة
# انسخ هذا الملف إلى .env ثم عدّل القيم عند الحاجة:
#   cp .env.example .env
# ============================================

# قاعدة البيانات SQLite (المسار نسبي لملف prisma/schema.prisma)
# تُنشأ قاعدة البيانات وملفها تلقائياً عند تنفيذ: npx prisma db push
DATABASE_URL=file:../db/custom.db
